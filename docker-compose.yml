version: '3.8'

services:
  finance-backend:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - finance-data:/app/data
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      
volumes:
  finance-data:
